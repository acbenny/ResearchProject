spring:
  application:
    name: ne-service
  cloud:
    consul:
      host: ${HOST_IP:localhost}
      discovery:
        instance-id: ${spring.cloud.client.hostname}
        ip-address: ${POD_IP}
        prefer-ip-address: true
      config:
        format: yaml